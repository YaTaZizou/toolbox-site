import { Metadata } from "next";
export const metadata: Metadata = { title: "Amélioration d'Image par IA", description: "Augmente la résolution de tes images jusqu'à 4x grâce à l'IA. Outil Premium ToolBox." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
