import { Metadata } from "next";
export const metadata: Metadata = { title: "Générateur de Bio IA", description: "Génère une bio percutante pour Instagram, TikTok, LinkedIn ou Twitter en quelques secondes grâce à l'IA." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
