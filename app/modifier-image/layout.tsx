import { Metadata } from "next";
export const metadata: Metadata = { title: "Modifier une Image en ligne", description: "Redimensionne, pivote ou retourne tes images gratuitement en ligne. Aucun logiciel requis." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
