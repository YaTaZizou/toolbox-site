import { Metadata } from "next";
export const metadata: Metadata = { title: "Modifier un PDF en ligne", description: "Ajoute du texte, pivote, supprime ou réorganise les pages de ton PDF directement en ligne. Gratuit." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
