import { Metadata } from "next";
export const metadata: Metadata = { title: "Outils PDF en ligne gratuits", description: "Fusionner, découper, protéger, convertir des PDFs en ligne. Gratuit, rapide et sans inscription." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
