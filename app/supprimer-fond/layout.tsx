import { Metadata } from "next";
export const metadata: Metadata = { title: "Supprimer le fond d'une image", description: "Supprime automatiquement l'arrière-plan de tes images grâce à l'IA. Résultat PNG transparent. Gratuit." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
